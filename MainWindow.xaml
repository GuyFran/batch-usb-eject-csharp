<Window x:Class="UsbEjector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="USB Batch Ejector" Height="480" Width="700">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Refresh button -->
        <Button Grid.Row="0" Content="Refresh Drives"
                Width="140" Height="32"
                Margin="0 0 0 10"
                Click="Refresh_Click"/>

        <!-- LIST -->
        <ListView x:Name="DrivesList"
                  Grid.Row="1"
                  Margin="0 0 0 10"
                  SelectionMode="Extended">
            <ListView.View>
                <GridView>

                    <GridViewColumn Header="Select" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Mount" DisplayMemberBinding="{Binding DriveLetter}" Width="80"/>
                    <GridViewColumn Header="NTFS Mount Path" DisplayMemberBinding="{Binding MountedPath}" Width="200"/>
                    <GridViewColumn Header="Label" DisplayMemberBinding="{Binding VolumeLabel}" Width="120"/>

                    <GridViewColumn Header="VID" DisplayMemberBinding="{Binding VendorId}" Width="60"/>
                    <GridViewColumn Header="PID" DisplayMemberBinding="{Binding ProductId}" Width="60"/>

                </GridView>
            </ListView.View>
        </ListView>

        <!-- Eject button -->
        <Button Grid.Row="2"
                Content="Eject Selected"
                Width="150" Height="36"
                HorizontalAlignment="Right"
                Click="Eject_Click"/>
    </Grid>
</Window>
